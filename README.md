# React Forms Demo

This is the starting code for the React Forms chapter in TripleTen's AI Software Engineering program.

## Tests

Tests for each lesson are found in `tests/` and can be run with `npm run test:l1`. Replace `1` with the lesson number.

## Solutions

Solutions can be found in the `solutions` branch.
